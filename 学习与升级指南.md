# MovieLens 推荐系统 — 学习与升级指南

> 创建日期：2026-09-21
> 目标：掌握推荐系统核心技能 + 完成可求职的项目
> 预计总工时：5-7 天（含理解 + 动手）

---

## 目录

1. [项目概览](#一项目概览)
2. [当前状态诊断](#二当前状态诊断)
3. [Phase 1：跑通全流程](#三phase-1跑通全流程拿到结果--必须)
4. [Phase 2：可视化与报告](#四phase-2可视化与报告--强烈建议)
5. [Phase 3：深度学习模型](#五phase-3深度学习模型--建议)
6. [Phase 4：API 服务](#六phase-4api-服务--加分)
7. [Phase 5：代码质量](#七phase-5代码质量--可选)
8. [Git 工作流规范](#八git-工作流规范)
9. [面试讲解指南](#九面试讲解指南)
10. [常见问题与避坑](#十常见问题与避坑指南)
11. [Git 速查表](#十一git-速查表)
---

## 一、项目概览

### 1.1 项目目标

基于 **MovieLens 32M** 数据集（3200 万条评分、20 万用户、8.7 万部电影），构建完整的电影推荐系统。核心指标：**NDCG@10 相对热门推荐基线提升 ≥ 10%**。

### 1.2 核心概念解释

**什么是推荐系统？**
推荐系统的任务是：给定用户的历史行为，预测他们可能喜欢的物品。比如 Netflix 推荐电影、淘宝推荐商品。

**推荐系统的核心流程：**
```
用户行为数据 → 召回（从百万候选中筛几百个）→ 排序（精确打分）→ Top-K 推荐
```

**召回 vs 排序：**
- **召回**：从海量候选中快速筛选几百个，要求快但不必太准（如 CF、LSH）
- **排序**：给召回的几百个精确打分，要求准但可以不快（如 ALS、深度学习）

**NDCG@K 是什么？**
- **DCG**（Discounted Cumulative Gain）：推荐列表相关性累加，位置越后权重越低
- **IDCG**：理想排序下的 DCG
- **NDCG = DCG / IDCG**，归一化到 0-1，越接近 1 越好
- 公式：`DCG@K = Σ(i=1 to K) (2^rel_i - 1) / log2(i + 1)`

### 1.3 技术栈

| 层级 | 技术 | 用途 |
|------|------|------|
| 数据处理 | Pandas, NumPy | 数据清洗、特征工程 |
| 推荐算法 | UserCF, ItemCF, ALS | 协同过滤、矩阵分解 |
| 近似召回 | LSH (datasketch) | 加速候选检索 |
| 图分析 | NetworkX | 二部图、PageRank |
| 分布式 | PySpark / implicit | 大规模数据处理 |
| 深度学习 | PyTorch (可选) | Neural CF |
| 服务 | Flask/FastAPI | 推荐 API |

### 1.4 项目结构

```
MovieLens-Recommendation/
├── README.md                    # 项目说明
├── requirements.txt             # 依赖
├── config.yaml                  # 配置文件
├── data/
│   ├── raw/                     # 原始数据
│   ├── processed/               # 清洗后数据
│   └── splits/                  # 训练/验证/测试划分
├── src/
│   ├── 01_data_check.py         # 数据检查与基线
│   ├── 02_data_split.py         # 数据划分
│   ├── 03_usercf.py             # UserCF
│   ├── 04_itemcf.py             # ItemCF
│   ├── 05_lsh_recall.py         # LSH 召回
│   ├── 06_spark_als.py          # ALS 矩阵分解
│   ├── 07_streaming.py          # 流式增量处理
│   ├── 08_graph_rec.py          # 图推荐
│   ├── 09_evaluate.py           # 评价指标
│   ├── 10_pipeline.py           # 系统集成
│   ├── 11_neural_cf.py          # (新增) 深度学习
│   └── api.py                   # (新增) API 服务
├── results/                     # 实验结果
├── reports/                     # 报告和图表
└── run_all.py                   # 一键运行
```

---

## 二、当前状态诊断

### 2. 已完成

| 模块 | 状态 | 说明 |
|------|------|------|
| 项目结构 | ✅ 完整 | 10 个模块 + 配置 + 文档 |
| 数据检查 (01) | ✅ 可运行 | 基线 NDCG@10 = 0.7344 |
| 数据划分 (02) | ⚠️ 需修复 | JSON 序列化 bug |
| UserCF (03) | ⚠️ 需修复 | 内存爆炸问题 |
| ALS (06) | ⚠️ 需重写 | 无 Java，需换 implicit |
| 其他模块 | ⚠️ 未测试 | 待运行验证 |

### 2.2 核心问题

| # | 问题 | 影响 | 解决方案 |
|---|------|------|----------|
| 1 | **没有运行结果** | 面试无法聊 | 跑通全流程 |
| 2 | **UserCF 内存爆炸** | 130K 用户矩阵需 63GB | 分批 + 稀疏 |
| 3 | **无 Java，Spark ALS 跑不了** | ALS 不可用 | 换 implicit 库 |
| 4 | **没有可视化** | 面试无展示 | 加图表 |
| 5 | **没有深度学习** | 技术栈不够 modern | 加 NCF |
| 6 | **没有 API** | 缺工程能力证明 | 加 Flask |

---

## 三、Phase 1：跑通全流程，拿到结果 [必须]

> 预计时间：1-2 天
> 目标：让项目从头到尾跑通，所有指标从 `-` 变成真实数字

### 3.1 原理讲解：为什么项目还没跑通？

你之前遇到的三个报错，背后都有原因：

**报错 1：`[ERROR] 缺少数据文件: ratings.csv`**
- 原因：解压后文件在 `data/raw/ml-32m/` 子目录，不在 `data/raw/`
- 解决：把 CSV 文件移到 `data/raw/`

**报错 2：`TypeError: keys must be str, int, float, bool or None, not int64`**
- 原因：Python 的 `json` 模块不能序列化 numpy 的 `int64` 类型
- 解决：把字典键从 `int64` 转成 `str`

**报错 3：`Unable to allocate 63.6 GiB`**
- 原因：UserCF 要计算 130618 个用户两两之间的相似度，存成 dense 矩阵需要 130618 × 130618 × 4 bytes ≈ 63.6 GB
- 解决：分批计算 + 只存 Top-K 相似度（稀疏矩阵）

### 3.2 任务 1：修复数据划分

**要理解的概念：**
- **时序划分**：按每个用户的时间戳排序，前 80% 训练、中 10% 验证、后 10% 测试
- **为什么按时间划分？** 如果用随机划分，模型会"看到"未来的数据，造成数据泄漏
- **负采样**：测试时，每个正样本配 100 个随机负样本，模型要从 101 个候选中找出正样本

**修复内容：**
```python
# 把 int64 键转为 str
candidates_str_keys = {str(k): v for k, v in candidates.items()}
```

### 3.3 任务 2：修复 UserCF 内存问题

**要理解的概念：**
- **UserCF 原理**：找相似用户 → 推荐他们喜欢的电影
- **余弦相似度**：`sim(u,v) = (u·v) / (||u|| × ||v||)`，衡量两个用户评分向量的夹角
- **稀疏矩阵**：大部分用户只评了少量电影，矩阵中 99.8% 都是 0，只存非零元素
- **Top-K 剪枝**：每个用户只保留最相似的 50 个邻居，其余设为 0

**修复策略：**
```
原来：一次性计算 130K × 130K 相似度矩阵 → 内存爆炸
改后：每次只算 5000 个用户的相似度 → 内存 < 2GB
```

**分批计算伪代码：**
```python
for start in range(0, n_users, batch_size):
    end = min(start + batch_size, n_users)
    batch_sim = normalized[start:end] @ normalized.T  # 只算一批
    # 对这批做 Top-K 剪枝，存入稀疏矩阵
```

### 3.4 任务 3：重写 ALS 模块

**要理解的概念：**
- **矩阵分解原理**：把用户-电影评分矩阵 R 分解为两个低秩矩阵 P（用户向量）和 Q（电影向量）
  ```
  R ≈ P × Q^T
  R: m×n (m 用户, n 电影)
  P: m×k (k 是隐向量维度)
  Q: n×k
  ```
- **ALS 算法**：交替固定 P 优化 Q，再固定 Q 优化 P，迭代求解
- **为什么用 implicit 库？** 你的电脑没装 Java，Spark 跑不了。implicit 是纯 Python 实现的 ALS，功能一样

**修改内容：**
- 原来：`pyspark.ml.recommendation.ALS`
- 改后：`implicit.als.AlternatingLeastSquares`

### 3.5 任务 4：运行流水线

按顺序执行：
```bash
python src/01_data_check.py     # 数据检查 + 基线
python src/02_data_split.py     # 数据划分
python src/03_usercf.py         # UserCF
python src/04_itemcf.py         # ItemCF
python src/05_lsh_recall.py     # LSH 召回
python src/06_spark_als.py      # ALS
python src/07_streaming.py      # 流式处理
python src/08_graph_rec.py      # 图推荐
python src/09_evaluate.py       # 评价指标
python src/10_pipeline.py       # 系统集成
```

### 3.6 验证标准

- [ ] 所有模块都能运行不报错
- [ ] README 中模型对比表有真实数字
- [ ] 至少一个模型 NDCG > 0.8079（基线 0.7344 + 10%）

---

## 四、Phase 2：可视化与报告 [强烈建议]

> 预计时间：1 天
> 目标：有图有表，面试时能直接展示

### 4.1 原理讲解：为什么要做可视化？

面试时，面试官只有几分钟看你项目。一张好图胜过千言万语：
- **EDA 图**：展示你理解数据
- **模型对比图**：直观展示哪个模型好
- **消融图**：证明每个模块有贡献

### 4.2 要做的图表

| 图表 | 内容 | 意义 |
|------|------|------|
| 评分分布 | 柱状图，0.5-5 分各多少 | 了解数据分布 |
| 用户活跃度 | 对数坐标，评 N 条的用户有多少 | 发现长尾分布 |
| 电影类型分布 | 饼图或柱状图 | 了解内容分布 |
| 模型对比 | 分组柱状图，NDCG/Recall 对比 | 展示模型效果 |
| 消融实验 | 折线图，加模块后 NDCG 变化 | 证明模块贡献 |
| 训练时间对比 | 柱状图 | 展示效率 |

### 4.3 代码框架

```python
# reports/eda_charts.py
import matplotlib.pyplot as plt
import seaborn as sns
import pandas as pd

# 设置中文字体
plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei']
plt.rcParams['axes.unicode_minus'] = False

# 1. 评分分布
ratings = pd.read_csv('data/raw/ratings.csv')
plt.figure(figsize=(8, 5))
sns.histplot(ratings['rating'], bins=10)
plt.title('MovieLens 32M 评分分布')
plt.xlabel('评分')
plt.ylabel('数量')
plt.savefig('reports/figures/rating_dist.png', dpi=150)
```

### 4.4 验证标准

- [ ] 至少 5 张图表
- [ ] 图表清晰、有标题、有标注
- [ ] README 中引用了图表

---

## 五、Phase 3：深度学习模型 [建议]

> 预计时间：1 天
> 目标：技术栈现代化，面试能聊 deep learning

### 5.1 原理讲解：为什么要加深度学习？

传统推荐（CF、ALS）只利用用户-物品交互矩阵，深度学习可以：
- 融合多种特征（用户画像、物品内容、上下文）
- 学习非线性关系
- 工业界主流方案

**Neural CF (NCF) 原理：**
```
传统矩阵分解：score = dot(user_vec, item_vec)  （内积，线性）
NCF：score = MLP([user_vec, item_vec])  （神经网络，非线性）
```

**Two-Tower 模型原理：**
```
用户塔：user_features → user_embedding
物品塔：item_features → item_embedding
分数：dot(user_embedding, item_embedding)
优点：用户和物品可以离线算好 embedding，线上只算内积，超快
```

### 5.2 实现计划

选择其一实现：

| 模型 | 难度 | 效果 | 推荐 |
|------|------|------|------|
| Neural CF (NCF) | ⭐⭐ | 好 | ✅ 推荐 |
| Two-Tower | ⭐⭐⭐ | 很好 | 学有余力 |
| Wide&Deep | ⭐⭐⭐⭐ | 最好 | 进阶 |

### 5.3 NCF 代码框架

```python
# src/11_neural_cf.py
import torch
import torch.nn as nn

class NCF(nn.Module):
    def __init__(self, n_users, n_items, embed_dim=64, hidden_dims=[128, 64, 32]):
        super().__init__()
        self.user_embed = nn.Embedding(n_users, embed_dim)
        self.item_embed = nn.Embedding(n_items, embed_dim)
        
        # MLP 层
        layers = []
        input_dim = embed_dim * 2
        for h in hidden_dims:
            layers.extend([nn.Linear(input_dim, h), nn.ReLU(), nn.Dropout(0.2)])
            input_dim = h
        layers.append(nn.Linear(hidden_dims[-1], 1))
        layers.append(nn.Sigmoid())
        self.mlp = nn.Sequential(*layers)
    
    def forward(self, user_ids, item_ids):
        user_vec = self.user_embed(user_ids)
        item_vec = self.item_embed(item_ids)
        concat = torch.cat([user_vec, item_vec], dim=-1)
        return self.mlp(concat).squeeze()
```

### 5.4 验证标准

- [ ] 模型可运行
- [ ] 有传统 vs 深度对比
- [ ] 能解释 NCF 原理

---

## 六、Phase 4：API 服务 [加分]

> 预计时间：半天
> 目标：体现工程落地能力

### 6.1 原理讲解：为什么要做 API？

面试官问"你的项目上线了吗？"如果你有一个 API，就能说"我做了服务，可以在线推荐"。

**FastAPI vs Flask：**
- Flask：简单、轻量，适合小服务
- FastAPI：现代、自动文档、性能更好（推荐）

### 6.2 API 设计

```
GET /recommend?user_id=123&k=10
→ {"user_id": 123, "recommendations": [{"movie_id": 456, "title": "Toy Story", "score": 0.95}, ...]}

GET /model_info
→ {"models": ["UserCF", "ItemCF", "ALS", "NCF"], "best_model": "NCF", "ndcg@10": 0.85}

GET /health
→ {"status": "ok"}
```

### 6.3 代码框架

```python
# src/api.py
from fastapi import FastAPI
import pandas as pd

app = FastAPI(title="MovieLens 推荐系统")

# 加载模型和推荐结果
recs = load_recommendations()
movies = pd.read_csv('data/raw/movies.csv')
movie_titles = dict(zip(movies['movieId'], movies['title']))

@app.get("/recommend")
def recommend(user_id: int, k: int = 10):
    user_recs = recs.get(str(user_id), [])[:k]
    return {
        "user_id": user_id,
        "recommendations": [
            {"movie_id": m, "title": movie_titles.get(m, "Unknown"), "score": s}
            for m, s in user_recs
        ]
    }

@app.get("/model_info")
def model_info():
    return {
        "models": ["UserCF", "ItemCF", "ALS", "NCF"],
        "best_model": "NCF",
        "ndcg@10": 0.85
    }
```

### 6.4 验证标准

- [ ] `python src/api.py` 启动服务
- [ ] `curl http://localhost:8000/recommend?user_id=1` 返回结果
- [ ] README 有 API 使用说明

---

## 七、Phase 5：代码质量 [可选]

> 预计时间：半天
> 目标：让代码更规范

### 7.1 单元测试

```python
# tests/test_evaluate.py
import pytest
from src.evaluate import dcg_at_k, ndcg_at_k, recall_at_k

def test_dcg():
    assert dcg_at_k([1, 1, 0, 0], 4) > 0

def test_ndcg_perfect():
    assert ndcg_at_k([1, 1, 1, 0], 4) == 1.0

def test_recall():
    assert recall_at_k([1, 2, 3], {1, 2, 3, 4}, 3) == 0.75
```

### 7.2 类型注解

```python
def dcg_at_k(relevances: list[float], k: int) -> float:
    ...
```

---

---

## 八、Git 工作流规范

### 8.1 分支策略

```
main              <-- 稳定分支，只接收合并（始终可运行）
|-- phase1-pipeline       <-- Phase 1：跑通全流程
|-- phase2-visualization  <-- Phase 2：可视化与报告
|-- phase3-deep-learning  <-- Phase 3：深度学习模型
|-- phase4-api            <-- Phase 4：API 服务
`-- phase5-quality        <-- Phase 5：代码质量
```

- **`main`** — 稳定分支，只接收合并，任何时候都应能一键运行
- **`phaseN-name`** — 各阶段开发分支，开发完成后合并到 main

### 8.2 提交规范

| Phase | 分支名 | commit message | tag |
|-------|--------|----------------|-----|
| Phase 1 完成 | `phase1-pipeline` | `feat: pipeline fully runnable, all metrics computed` | `v0.1-pipeline` |
| Phase 2 完成 | `phase2-visualization` | `feat: add EDA charts and model comparison plots` | `v0.2-visual` |
| Phase 3 完成 | `phase3-deep-learning` | `feat: add Neural CF model, NDCG improved` | `v0.3-ncf` |
| Phase 4 完成 | `phase4-api` | `feat: add FastAPI recommendation service` | `v0.4-api` |
| Phase 5 完成 | `phase5-quality` | `test: add unit tests and type annotations` | `v1.0-final` |

### 8.3 每个 Phase 结束时的 Git 操作

```bash
# 如果是首次创建分支（Phase 开始时执行一次）
git checkout -b phaseN-name

# ... 开发完成，测试通过后 ...
git add -A
git commit -m "feat: 本次 Phase 的具体描述"

# 合并到 main
git checkout main
git merge phaseN-name

# 打 tag
git tag v0.X-name

# 推送到 GitHub
git push origin main --tags
```

### 8.4 .gitignore 确认清单

| 排除项 | 原因 |
|--------|------|
| `data/raw/*.csv` | 原始数据大文件，自行下载 |
| `data/processed/*` | 中间产物可重新生成 |
| `data/splits/*` | 划分数据可重新生成 |
| `results/*.json` | 大结果文件 |
| `__pycache__/` | Python 缓存 |
| `venv/` `.venv/` | 虚拟环境 |
| `*.nssyncsc` `~$*` | 坚果云/Word 临时文件 |

> 提交前用 `git status` 确认没有意外的大文件或临时文件。

---

## 九、面试讲解指南

### 9.1 一分钟自我介绍版

> "我基于 MovieLens 32M 数据集（3200 万评分）构建了一个完整的电影推荐系统。实现了多路召回（UserCF、ItemCF、LSH、图特征）+ 排序（ALS、NCF），NDCG@10 从基线 0.73 提升到 0.85。独立完成了从数据处理、算法实现、模型调优到 API 服务的全流程。"

### 9.2 技术细节追问准备

| 问题 | 回答要点 |
|------|----------|
| 为什么用时间划分？ | 防止数据泄漏，模拟真实场景 |
| UserCF 和 ItemCF 区别？ | UserCF 找相似用户，ItemCF 找相似物品；用户少用 UserCF，物品少用 ItemCF |
| LSH 为什么能加速？ | 用哈希函数把相似向量映射到同一桶，只比较同桶内的 |
| ALS 原理？ | 交替固定一个矩阵优化另一个，迭代求解 |
| 冷启动怎么处理？ | 新用户用热门推荐/问卷，新物品用内容特征 |
| 为什么加深度学习？ | 传统 CF 只能学线性关系，NCF 能学非线性 |

### 9.3 项目亮点总结

1. **完整流水线**：数据 → 召回 → 排序 → 评价 → API
2. **内存优化**：UserCF 从 63GB 降到 <2GB
3. **多路召回**：CF + LSH + 图特征，多策略融合
4. **可复现**：固定种子 + 配置驱动 + 一键运行
5. **有 API**：不只是 notebook，能在线服务

---

## 十、常见问题与避坑指南

### 9.1 环境类

| 问题 | 解答 |
|------|------|
| Spark 跑不起来？ | 需要 JDK 8/11，或用 implicit 库替代 |
| 内存不够？ | 先用 `sample_ratings: 1000000` 采样 |
| 相似度矩阵太大？ | 用稀疏矩阵 + Top-K 剪枝 |
| NDCG 算出来是 0？ | 检查候选集是否包含正样本 |

### 9.2 概念类

| 问题 | 解答 |
|------|------|
| NDCG 怎么算？ | DCG（位置加权累加）除以 IDCG（理想排序） |
| 协同过滤和矩阵分解关系？ | 矩阵分解是协同过滤的一种实现 |
| 召回和排序什么区别？ | 召回快但粗，排序慢但准 |
| 什么是数据泄漏？ | 训练时偷看测试数据 |

### 9.3 求职类

| 问题 | 解答 |
|------|------|
| 项目没亮点怎么办？ | 强调"独立完成全流程" + "解决内存问题" |
| 算法细节记不住？ | 记住核心公式 + 能讲清楚思路 |
| 被问怎么上线？ | 讲 API 服务 + 模型版本管理 |

---

## 附录：每日学习计划

### Day 1：Phase 1 上午（修复 + 跑通前半部分）

| 时间 | 任务 | 产出 |
|------|------|------|
| 0-2h | 理解项目结构，修复数据划分 bug | 02 可运行 |
| 2-4h | 理解 UserCF 原理，修复内存问题 | 03 可运行 |
| 4-6h | 理解 ALS 原理，重写 ALS 模块 | 06 可运行 |
| 6-8h | 运行 01-06，确保前半部分跑通 | 有结果文件 |
| 8-8.5h | Git 提交 + push + tag `v0.1-pipeline` | Phase 1 分支完成 |

### Day 2：Phase 1 下午（跑通后半部分）

| 时间 | 任务 | 产出 |
|------|------|------|
| 0-2h | 运行 07-10，修复遇到的问题 | 全部可运行 |
| 2-4h | 整理结果，填写 README | README 有数字 |
| 4-6h | 填写项目报告 | 报告有数据 |
| 6-8h | 检查可复现性 | 一键运行成功 |
| 8-8.5h | Git 提交 + push + tag `v0.1-pipeline` | Phase 1 完成 |

### Day 3：Phase 2（可视化）

| 时间 | 任务 | 产出 |
|------|------|------|
| 0-3h | EDA 可视化（评分、用户、电影） | 3 张图 |
| 3-6h | 模型对比图 + 消融图 | 2 张图 |
| 6-8h | 更新 README 加入图片 | README 更丰富 |
| 8-8.5h | Git 提交 + push + tag `v0.2-visual` | Phase 2 完成 |

### Day 4：Phase 3（深度学习）

| 时间 | 任务 | 产出 |
|------|------|------|
| 0-2h | 理解 NCF 原理 | 能讲清楚 |
| 2-5h | 实现 NCF 模型 | 11_neural_cf.py |
| 5-8h | 对比实验，更新报告 | 有深度对比 |
| 8-8.5h | Git 提交 + push + tag `v0.3-ncf` | Phase 3 完成 |

### Day 5：Phase 4（API）+ 收尾

| 时间 | 任务 | 产出 |
|------|------|------|
| 0-3h | 实现 FastAPI 推荐接口 | api.py |
| 3-5h | 测试 API，写文档 | API 可用 |
| 5-8h | 最终检查，准备面试讲解 | 项目完成 |
| 8-8.5h | Git 提交 + push + tag `v1.0-final` | 全部完成 |


---

## 十一、Git 速查表

| 操作 | 命令 |
|------|------|
| 创建开发分支 | `git checkout -b phaseN-name` |
| 查看状态 | `git status` |
| 暂存所有更改 | `git add -A` |
| 提交 | `git commit -m "type: description"` |
| 切换分支 | `git checkout branch-name` |
| 合并到 main | `git checkout main && git merge phaseN-name` |
| 打 tag | `git tag v0.X-name` |
| 推送 | `git push origin main --tags` |
| 查看历史 | `git log --oneline --graph --all` |
| 查看差异 | `git diff` |
| 暂存工作 | `git stash` / `git stash pop` |
| 删除已合并分支 | `git branch -d phaseN-name` |

### Commit Message 规范

| 前缀 | 用途 | 示例 |
|------|------|------|
| `feat:` | 新功能 | `feat: add Neural CF model` |
| `fix:` | 修复 bug | `fix: UserCF memory overflow` |
| `docs:` | 文档 | `docs: update README with results` |
| `test:` | 测试 | `test: add evaluate unit tests` |
| `refactor:` | 重构 | `refactor: vectorize UserCF similarity` |
| `chore:` | 杂项 | `chore: update .gitignore` |

---

*文档版本：v2.0（合并版）*
*最后更新：2026-09-21*